# 问题修复日志 (2026-03-06)

## 🐛 问题 1: 流式输出显示 [object Object]

### 症状
- 流式输出显示 `[object Object]`
- 消息还是等所有内容再一次性输出

### 根本原因
1. **消息格式未正确解析**：OpenClaw 返回的 `message` 是对象类型，不是纯字符串
2. **类型转换过早**：在 `handleChatEvent` 中直接调用 `String(message)` 导致对象被序列化为 `[object Object]`
3. **formatMessage 缺少处理**：原始的 `formatMessage` 函数对对象类型的消息处理不完善

### 修复方案

#### 1. 改进 formatMessage 函数 (chat-widget.js 第 1956-2013 行)

**旧代码：**
```javascript
formatMessage(content) {
    let text = '';
    if (typeof content === 'string') {
        text = content;
    } else if (Array.isArray(content?.content)) {
        text = content.content.map(block => {
            if (block.type === 'text') {
                return block.text || '';
            }
            return '';
        }).join('');
    } else {
        text = JSON.stringify(content); // ❌ 导致 [object Object]
    }
    // ...
}
```

**新代码：**
```javascript
formatMessage(content) {
    let text = '';

    if (typeof content === 'string') {
        text = content;
    } else if (content === null || content === undefined) {
        text = '';
    } else if (typeof content === 'object') {
        // ✅ 智能处理各种对象格式
        if (Array.isArray(content?.content)) {
            // OpenClaw 标准格式：{ content: [{ type: 'text', text: '...' }] }
            text = content.content.map(block => {
                if (block.type === 'text') {
                    return block.text || '';
                }
                return ''; // 跳过非文本块
            }).join('');
        } else if (content.text) {
            // 简单格式：{ text: '...' }
            text = content.text;
        } else if (content.message) {
            // 嵌套格式：{ message: '...' } 或 { message: { text: '...' } }
            text = typeof content.message === 'string'
                ? content.message
                : content.message?.text || '';
        } else {
            // ✅ 未知格式，显示友好提示而不是 [object Object]
            console.warn('⚠️ Unknown message format:', content);
            text = '[无法显示的消息内容]';
        }
    } else {
        text = String(content);
    }
    // ...
}
```

**支持的消息格式：**
- ✅ 字符串：`"hello"`
- ✅ 标准格式：`{ content: [{ type: 'text', text: 'hello' }] }`
- ✅ 简单格式：`{ text: 'hello' }`
- ✅ 嵌套格式：`{ message: 'hello' }` 或 `{ message: { text: 'hello' } }`
- ✅ 其他：显示友好提示

#### 2. 改进调试日志 (chat-widget.js 第 1536 行)

**新增调试信息：**
```javascript
console.log('📨 Chat event:', {
    state,
    status,
    runId,
    seq,
    hasMessage: !!message,
    messageType: typeof message,              // ✅ 显示消息类型
    messageKeys: message ? Object.keys(message) : null,  // ✅ 显示对象键
    messagePreview: message ? JSON.stringify(message).substring(0, 100) : null  // ✅ 完整预览
});
```

**作用：**
- 快速识别消息格式
- 帮助调试未来的消息格式变化

---

## 🐛 问题 2: 对话框显示 base64 内容

### 症状
- 用户上传文件后，对话框显示完整的 base64 编码数据
- base64 数据非常长，影响用户体验

### 根本原因
在 `sendMessage` 函数中，文件附件被添加到消息中，然后这个包含 base64 的消息被显示给用户。

**原始代码 (chat-widget.js 第 1665-1686 行):**
```javascript
if (this.uploadedFiles.length > 0) {
    message = message || '请帮我分析这些文件';

    let fileMessage = message;

    if (this.uploadedFiles.length === 1) {
        const file = this.uploadedFiles[0];
        fileMessage = `${message}\n\n📎 附件: ${file.name}\n📋 类型: ${file.type}\n📦 大小: ${this.formatFileSize(file.size)}\n📄 Base64 数据:\n${file.base64}`;
        // ❌ base64 会被显示
    }

    message = fileMessage;
}

// ...
this.appendMessage('user', message);  // ❌ 显示包含 base64 的消息
this.emit('message', { role: 'user', message });
```

### 修复方案

**创建两个消息版本：**
1. `displayMessage` - 用于显示（不包含 base64）
2. `message` - 用于发送（包含 base64）

**新代码 (chat-widget.js 第 1665-1720 行):**
```javascript
// 如果有文件附件，附加到消息中
let displayMessage = message; // ✅ 用于显示的消息（不包含 base64）
if (this.uploadedFiles.length > 0) {
    message = message || '请帮我分析这些文件';

    let fileMessage = message;
    let displayFileMessage = message;

    if (this.uploadedFiles.length === 1) {
        const file = this.uploadedFiles[0];
        // 发送给服务器的消息（包含 base64）
        fileMessage = `${message}\n\n📎 附件: ${file.name}\n📋 类型: ${file.type}\n📦 大小: ${this.formatFileSize(file.size)}\n📄 Base64 数据:\n${file.base64}`;
        // ✅ 显示给用户的消息（不包含 base64）
        displayFileMessage = `${message}\n\n📎 附件: ${file.name}\n📋 类型: ${file.type}\n📦 大小: ${this.formatFileSize(file.size)}`;
    } else {
        // 多个文件，同样创建两个版本
        fileMessage = `${message}\n\n📎 附件 (${this.uploadedFiles.length} 个文件):\n\n`;
        displayFileMessage = `${message}\n\n📎 附件 (${this.uploadedFiles.length} 个文件):\n\n`;

        this.uploadedFiles.forEach((file, index) => {
            fileMessage += `${index + 1}. ${file.name}\n   类型: ${file.type}\n   大小: ${this.formatFileSize(file.size)}\n   Base64:\n   ${file.base64}\n\n`;
            displayFileMessage += `${index + 1}. ${file.name}\n   类型: ${file.type}\n   大小: ${this.formatFileSize(file.size)}\n\n`;
        });
    }

    this.uploadedFiles = [];
    this.renderFileList();

    message = fileMessage;
    displayMessage = displayFileMessage;
}

// ...

// ✅ 显示用户消息（使用不包含 base64 的版本）
this.appendMessage('user', displayMessage);
this.emit('message', { role: 'user', message: displayMessage });
```

### 修复效果

**修复前：**
```
请帮我分析这个文件

📎 附件: test.txt
📋 类型: text/plain
📦 大小: 12 B
📄 Base64 数据:
SGVsbG8gV29ybGQhCg==
```

**修复后：**
```
请帮我分析这个文件

📎 附件: test.txt
📋 类型: text/plain
📦 大小: 12 B
```

✅ base64 数据仍然发送给服务器，但不显示给用户

---

## 📊 修改总结

### 修改的文件
- `chat-widget.js` (3 处修改)

### 具体修改
| 行号 | 函数 | 修改内容 |
|------|------|----------|
| 1536-1545 | `handleChatEvent` | 改进调试日志，显示消息结构 |
| 1665-1720 | `sendMessage` | 创建 displayMessage 版本，过滤 base64 |
| 1956-2013 | `formatMessage` | 支持多种消息格式，避免 [object Object] |

### 代码质量
- ✅ JavaScript 语法检查通过
- ✅ 向后兼容（支持旧格式）
- ✅ 添加调试日志
- ✅ 友好的错误提示

---

## 🧪 测试验证

### 测试 1: 流式显示
```bash
# 访问
http://8.148.244.33:8080/simple-integration.html?id=testuser

# 发送简单消息
"1+1="

# 预期结果
✅ 不再显示 [object Object]
✅ 显示实际文本内容
✅ 流式逐步显示（如果支持）
```

### 测试 2: 文件上传
```bash
# 上传一个文本文件
# 预期结果
✅ 对话框不显示 base64 数据
✅ 只显示文件名、类型、大小
✅ 服务器仍能收到 base64 数据
```

### 测试 3: 调试日志
```javascript
// 打开浏览器控制台
// 查看消息日志

console.log('📨 Chat event:', {
    messageType: typeof message,
    messageKeys: Object.keys(message),
    messagePreview: '...'
});
```

---

## 🚀 后续工作

1. **观察流式输出** - 确认是否真正逐步显示
2. **测试各种消息格式** - 确保 formatMessage 正确处理
3. **测试文件上传** - 验证 base64 过滤
4. **检查控制台** - 查看新的调试日志

---

**准备就绪，可以开始测试！** 🎯
